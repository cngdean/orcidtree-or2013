<%= form_for(@orcid) do |f| %>
  <% if @orcid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orcid.errors.count, "error") %> prohibited this orcid from being saved:</h2>

      <ul>
      <% @orcid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
